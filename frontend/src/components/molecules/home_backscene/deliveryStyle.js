import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 200,
    position: 'absolute',
    top: '30%',
    left: 0,
  },

  background: {
    width: '100%',
    height: '100%',
  },
});
export default styles;
