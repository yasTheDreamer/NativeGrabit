import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    position: 'relative',
  },
  background: {
    width: '100%',
    height: '80%',
    resizeMode: 'stretch',
  },
});

export default styles;
